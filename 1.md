#1.1 Пример - Ardupilot, набор модулей для управления БПЛА, написан на C++.
Предположительные основные классы 
(для описания железа)
- Machine (Vehicle/Drone/Copter..)
- Sensor (и подклассы - Gyroscope, Barometer, GPS, Lidar, Sonar, Radar)
- Motor (Actuator/Rotor)
- Battery
- GroundControlStation
#1.2 Если представлять себе, как эти классы выглядят:
```python
class Rover extends Machine {
  type = rover # plane, fixedwing, submarine, copter
  mass = 8,5 # in kilograms
  num_of_wheels = 6
  acce = new Acceleromenter()
  motor = new Motor()
  battery = new LiIon_18650()
  start() {
    if (!battery.isLow) {
        motor.startMotor()
        acce.startAccelerometer()
        }
    }
}
class Accelerometer extends Sensor {
  needsCalibration = False
  lastValue = 37
  currentValue = 0
  startAccelerometer()
}
class Motor {
  mass = 1.2
  enginePower = 1.5 # watts
  startMotor() {
  }
}
class LiIon_18650 extends Battery {
  mass =
  capacity = 3500
  is_low = False
}
```
